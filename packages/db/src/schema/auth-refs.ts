// BetterAuth table re-exports.
// Full definitions live in auth.ts. This file exists for backwards compatibility
// with existing imports that reference auth-refs.ts.
export { organization, user } from "./auth";
